import styled from 'styled-components';

const SearchBarSkillStyled = styled.div`
  display: flex;
  justify-content: flex-start;
  align-items: center;
  margin-bottom: 10px;
`;

export default SearchBarSkillStyled;
